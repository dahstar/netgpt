# apps.py

from django.apps import AppConfig

class NetgptConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Netgpt'  # Set the correct app name here (same as your main project)
